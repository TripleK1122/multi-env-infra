name: 🌍 Terraform Multi-Env Validation

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  terraform-dev:
    name: Terraform Validate & Plan (DEV)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: env/dev
    steps:
      - name: 🧰 Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      - name: 🔍 Terraform Init
        run: terraform init

      - name: ✅ Terraform Validate
        run: terraform validate

      - name: 📋 Terraform Format Check
        run: terraform fmt -check

      - name: 🧠 Terraform Plan
        run: terraform plan -no-color

  terraform-prod:
    name: Terraform Validate & Plan (PROD)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: env/prod
    steps:
      - name: 🧰 Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      - name: 🔍 Terraform Init
        run: terraform init

      - name: ✅ Terraform Validate
        run: terraform validate

      - name: 📋 Terraform Format Check
        run: terraform fmt -check

      - name: 🧠 Terraform Plan
        run: terraform plan -no-color
